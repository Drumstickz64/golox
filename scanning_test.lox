// this is a comment
(( )){} // grouping stuff
!*+-/=<> <= == // operators
"hello, world!" // this is a string
"this is
a multiline string"

var hello = "world" + "foo"+"bar" /* this is
a multiline comment
you can't nest them so this /* doesn't mean anything */

var foo = (1 + 2) * /* comment in the middle of code */ (3 / 4)

true ? 1 : 2